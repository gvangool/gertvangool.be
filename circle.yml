checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive

machine:
  pre:
    - git config --global user.name "CircleCI"
    - git config --global user.email "circleci@circleci.com"

dependencies:
  pre:
    - wget -O ~/tmp/hugo_0.20_linux-64bit.tgz https://github.com/spf13/hugo/releases/download/v0.20/hugo_0.20_Linux-64bit.tar.gz
    - cd ~/tmp && tar xzf hugo_0.20_linux-64bit.tgz
    - mv ~/tmp/hugo_0.20_linux_amd64/hugo_0.20_linux_amd64 ~/bin/hugo

test:
  override:
    - hugo -v

deployment:
  master:
    branch: master
    commands:
      - rm -rf public
      - git clone git@github.com:gvangool/gvangool.github.io.git public
      - ./deploy.sh
